# ScreenPlay Scripts

This folder is consumed by `Angene.GameWorkspace.csproj`.

- Add all game scripts as `*.cs` files in this directory.
- Open `Angene.GameWorkspace.csproj` in Visual Studio for IntelliSense and unified references.
- Launch `Angene.Editor` for a Unity-like multi-pane editing interface and script generation.
